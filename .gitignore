# Dependencies & build artifacts
node_modules/
_release_stage/
_release_stage*/
dist-out/
dist-release/
*.exe
*.zip
*.rar

# Local / personal data
config.json
freeclaude.bat
launch-claude.cmd
.env
.env.*

# Scratch / debug files. These have held live OAuth device codes,
# client secrets and session cookies, so the pattern is deliberately broad.
_*

# OS & editor
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# npm debug
npm-debug.log*
