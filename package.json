{
  "name": "freeclaude-code",
  "version": "1.0.5",
  "description": "Local Windows companion UI for Claude Code + OmniRoute + Kiro",
  "private": true,
  "bin": "server.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/kolevans/FreeClaudeCode.git"
  },
  "author": "kolevans",
  "license": "MIT",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check omni-keys.js && node --check omni-keys-proxy.js && node --check node-resolve.js && node --check format-duration.js && node --check sqlite-bridge.js && node --check public/app.js",
    "build": "powershell -ExecutionPolicy Bypass -File build-release.ps1"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "better-sqlite3": "13.0.3"
  },
  "pkg": {
    "targets": ["node22-win-x64"],
    "assets": [
      "public/**/*"
    ],
    "scripts": [
      "omni-keys-proxy.js",
      "node-resolve.js",
      "format-duration.js"
    ]
  }
}
